/**
 * 
 */
package constant;

/**
 * Enum for TB_INCLUSION_STATE
 * @author tmolina
 * @since 18-04-2011
 */
public enum InclusionState {
	
	NEW(1L),
	SENT(2L),
	DETAILS_INITIALIZED(3L);

	private Long id;

	/**
	 * 
	 * @param id
	 */
	private InclusionState(Long id) {
	  this.id = id;
	}

	/**
	 * 
	 * @return id
	 */
	public Long getId() {
	 	 return id;
	}
}