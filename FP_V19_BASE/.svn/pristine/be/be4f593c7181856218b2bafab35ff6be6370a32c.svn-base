package ejb.balance;

import javax.ejb.Remote;

@Remote
public interface TimerBalance {
	
	public void createTimer();

	Long umbralActual(Long accountId);

	Long saldoActualCta(Long accountId, int tipoCta);
	
}
