/**
 * 
 */
package constant;

/**
 * Enum for TB_OPERATION_TYPE
 * @author tmolina
 * @since 22-02-2011
 */
public enum OperationType {
	
	RECHARGE(1L),
	NEW(2L),
	REPLACEMENT(3L), 
	TRANSFER(4L);

	 private Long id;

	 /**
	  * 
	  * @param id
	  */
	 private OperationType(Long id) {
	   this.id = id;
	 }

	 /**
	  * 
	  * @return id
	  */
	 public Long getId() {
	  	 return id;
	 }
}